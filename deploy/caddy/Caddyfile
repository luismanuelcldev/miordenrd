api.monitoringsystemtest.live {
  encode zstd gzip

  @api path /api/*
  handle @api {
    uri strip_prefix /api
    reverse_proxy backend-staging:3000
  }

  # Health local
  respond /health "healthy" 200
}