// Loader dedicado para la sección de usuarios en el panel de administración
import { PageLoading } from "@/components/ui/loading-spinner"

export default function Loading() {
  return <PageLoading text="Cargando usuarios..." />
}

