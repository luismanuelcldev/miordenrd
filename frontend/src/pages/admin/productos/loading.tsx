// Loader para la vista de gestión de productos en el panel admin
import { PageLoading } from "@/components/ui/loading-spinner"

export default function Loading() {
  return <PageLoading text="Cargando productos..." />
}

