// Loader para la vista de gestión de pedidos en el panel admin
import { PageLoading } from "@/components/ui/loading-spinner"

export default function Loading() {
  return <PageLoading text="Cargando pedidos..." />
}

