// Muestro un spinner de página mientras se cargan los productos del catálogo
import { PageLoading } from "@/components/ui/loading-spinner"

export default function Loading() {
  return <PageLoading text="Cargando productos..." />
}

